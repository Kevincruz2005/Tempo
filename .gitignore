# secrets — NEVER commit
.env
.env.*
!.env.example

# dependencies
node_modules/

# runtime state (regenerable; curated evidence lives in test/reports)
journal/

# local artifacts
port-codex.zip
release/
packages/core/dist/
coverage/
*.log
.DS_Store
