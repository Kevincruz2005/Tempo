# End-to-End Suite

Run `TEMPO_DRY_RUN=false npm run test:e2e` with two distinct funded Shannon
keys. The runner operates the full firm against live windows and records
observed lifecycle states and real receipt hashes in
`test/reports/e2e-live.md`.

