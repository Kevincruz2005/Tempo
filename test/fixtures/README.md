# Fixtures

Production code does not consume fixtures. Captured live evidence is retained
under `docs/sources/evidence/` and `port-codex/live-evidence/` with capture
dates and endpoint provenance. Synthetic unit inputs are mathematical fixtures
only and are labeled as such in the tests.

